#pragma once

#include <string>
#include <tuple>
#include <vector>
#include <iostream>

using namespace std;

class Signup {
public:
    tuple<string, string, string> signup(string, string, string);
};